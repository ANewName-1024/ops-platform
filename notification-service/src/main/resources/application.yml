server:
  port: 8094
  address: 0.0.0.0

spring:
  application:
    name: notification-service
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: localhost:8848
  datasource:
    url: jdbc:postgresql://8.137.116.121:8432/business_db
    username: business
    password: NewPass2024
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET:NotificationServiceJWTSecretKey2024}
  expiration: 86400000

logging:
  level:
    com.example.notification: INFO
